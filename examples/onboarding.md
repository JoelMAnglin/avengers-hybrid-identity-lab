# Onboarding example

Run `11-New-Avenger.ps1 -RequestId REQ0001001 -FirstName Miles -LastName Morales
-Username mmorales -Department Engineering -Title 'Junior Systems Engineer'
-Manager pparker -WhatIf`, review, then rerun without `-WhatIf`. Validate object,
OU, manager, groups, password-change flag, audit record, and optional cloud match.

