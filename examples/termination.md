# Termination example

With authorization, preview then run `13-Disable-Avenger.ps1`; investigate active
sessions and cloud controls. Next preview/run `14-Terminate-Avenger.ps1`. Preserve
the membership export, validate disabled state/OU/access removal, and document
mailbox, devices, licenses, tokens, data ownership, and legal-hold actions outside
the on-prem script.

