# Department transfer example

Run `12-Move-Avenger.ps1 -Username pparker -NewDepartment Security -NewTitle
'Identity Security Analyst' -RequestId REQ0001002 -WhatIf`. Capture before/after
memberships, approve, execute, then validate OU, attributes, removed Engineering
roles, added Security roles, and downstream access.

